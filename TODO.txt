* maybe plugin-able additional builtin types? like:
	* json
	* glm vecs
	* filesystem path
	* other types you might have in your code-base
	but then how to use them in different languages?
* break Schema's dependend on Database (i.e. the friend declarations)